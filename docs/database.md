# Database

WyFlask does not strictly couple you to a specific ORM or database. 
You can use SQLAlchemy, MongoDB, or raw SQL.

We provide a `Repository` base class in `wyflask.repositories` to encourage data access abstraction.
